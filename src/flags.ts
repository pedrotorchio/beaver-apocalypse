export default {
    /** When true, draw collision shapes (e.g. projectile radius, ground check circles). */
    renderCollisionBoundaries: true,
    bounceOffMode: false,
    waitPhysicsSettling: false,
    /** Numerical index matching the player index. NaN for all AI players, Infinity for all human players. */
    playerIndex: NaN,
    beaverCount: 4
}